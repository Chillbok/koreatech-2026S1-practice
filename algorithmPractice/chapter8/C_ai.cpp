// 이 문제는 '탐욕적 알고리즘(Greedy Algorithm)'을 사용하여 해결합니다.
// 매 순간 가장 최선이라고 생각되는 선택(가장 싼 사탕을 사는 것)을 하면 전체적인 최적해(가장 많은 사탕을 사는 것)에 도달한다는 원리입니다.
// ※ 제출 시 알고리즘의 정확성 증명은 별도 pdf 파일로 제출해야 합니다.

#include<iostream> // 입출력을 위한 표준 라이브러리입니다. cin(입력), cout(출력) 등을 사용할 수 있게 해줍니다.
using std::cin; using std::cout; using std::endl;

#include<vector> // 크기가 자유롭게 변할 수 있는 배열인 'vector'를 사용하기 위한 라이브러리입니다.
using std::vector;

#include<algorithm> // 정렬(sort)과 같은 다양한 알고리즘 함수들을 사용하기 위한 라이브러리입니다.

// [문제 설명]
// 다양한 사탕이 있고 각 사탕의 가격이 주어졌을 때, 한정된 예산으로 최대한 많은 사탕을 구입하려고 합니다.
// 주어진 예산 안에서 최대 몇 개의 사탕을 살 수 있는지 구하는 함수입니다.

void test() {
	// M: 현재 가지고 있는 예산 (1 이상, 100 이하)
	// N: 상점에 있는 사탕의 총 개수 (1 이상 100 이하)
	int M, N; 
	cin >> M >> N; // 사용자로부터 예산(M)과 사탕 개수(N)를 입력받습니다.

	// 사탕 N개의 가격을 저장할 공간을 만듭니다. 'vector'는 유연한 배열 역할을 합니다.
	vector<int> testcase(N);
	
	// N개의 사탕 가격을 차례대로 입력받아 vector(배열) 안에 저장합니다.
	for (int& x : testcase) cin >> x;

	// 최대한 많은 사탕을 사려면 '가장 저렴한 사탕'부터 구매하는 것이 유리합니다.
	// 따라서, std::sort 함수를 이용해 입력받은 사탕 가격들을 오름차순(싼 가격부터 비싼 가격 순)으로 정렬합니다.
	std::sort(testcase.begin(), testcase.end());
	
	int count = 0; // 내가 구매한 사탕의 개수를 세기 위한 변수입니다. 처음엔 0개부터 시작합니다.
	
	// 정렬된 사탕 가격들을 순서대로(가장 싼 것부터) 하나씩 확인합니다.
	for (int price : testcase) {
		// 남은 예산(M)이 현재 확인 중인 사탕의 가격(price)보다 크거나 같다면 살 수 있습니다.
		if (M >= price) {
			M -= price; // 예산에서 사탕 가격만큼 돈을 뺍니다. (M = M - price 와 같은 의미)
			count++;    // 사탕을 하나 샀으므로 구매 개수를 1 증가시킵니다.
		} else {
			// 남은 예산보다 사탕 가격이 더 비싸면 더 이상 살 수 없습니다.
			// 이미 사탕 가격이 오름차순으로 정렬되어 있으므로, 다음 사탕들은 당연히 더 비싸서 살 수 없습니다.
			// 따라서 반복문을 즉시 멈추고 빠져나옵니다. (break)
			break;
		}
	}
	
	// 최종적으로 구매한 사탕의 개수를 화면에 출력하고 줄바꿈(endl)을 합니다.
	cout << count << endl;
}

int main() {
	// T: 테스트 케이스의 개수 (프로그램을 몇 번 반복해서 실행할 것인지)
	int T; 
	cin >> T; // 테스트 케이스 개수를 입력받습니다.
	
	// T가 0이 될 때까지 (즉, T번 만큼) test() 함수를 반복해서 실행합니다.
	// while(T--)는 T를 1씩 줄여가면서 반복하다가 0이 되면 멈추는 짧고 흔한 C++ 문법입니다.
	while(T--) test();
}

/*
입력 예시:
3          <- 테스트 케이스 개수 (총 3번 검사)
7 5        <- 첫 번째: 예산 7, 사탕 5개
1 3 2 4 1  <- 사탕 가격들 (1원짜리부터 정렬 후 구매 시도)
5 6        <- 두 번째: 예산 5, 사탕 6개
10 6 8 7 7 8 <- 사탕 가격들 (가장 싼 사탕이 6원이므로 예산 부족)
20 6       <- 세 번째: 예산 20, 사탕 6개
1 6 3 1 2 5 <- 사탕 가격들

출력 설명: 각 테스트케이스마다 주어진 예산 내에서 구매할 수 있는 최대 사탕 수를 출력해야 함.
출력 예시:
4  <- 첫 번째: 1원, 1원, 2원, 3원짜리 총 4개 (총 7원)
0  <- 두 번째: 예산 부족으로 하나도 못 삼
6  <- 세 번째: 모든 사탕을 다 사도 18원이라 6개 모두 삼
*/