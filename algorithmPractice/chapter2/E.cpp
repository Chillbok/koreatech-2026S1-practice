#include <iostream>
#include <vector>
using std::cin, std::cout, std::endl;
using std::vector;

bool solve() {
    
}

void test() {
    int var_count;
    cin >> var_count;
    // 값 입력받고, 그 입력받은 값을 기반으로 계산하는 solve() 함수 아래에서 선언할 것
}

int main() {
    int testcase_count;
    cin >> testcase_count;
    while (testcase_count--) test();
    return 0;
}