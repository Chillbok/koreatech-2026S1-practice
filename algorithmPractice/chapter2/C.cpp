#include <iostream>
#include <vector>
using std::cin;
using std::cout;
using std::endl;
using std::vector;

vector<int>* solve() {

}

int main() {
    int testcase_count;
    cin >> testcase_count;
    
    while (testcase_count--) {
        int M, N;
        vector<int> testcase(N);
        for (int& idx : testcase) cin >> idx;
        // 경우의 수 출력
    }
    
    return 0;
}