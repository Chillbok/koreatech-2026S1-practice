#include <iostream>
using std::cin; using std::cout; using std::endl;
#include <vector>
using std::vector;

int main() {
    int testcase_count;
    if (!(cin >> testcase_count)) return 0;
    
    return 0;
}